import React from 'react';

type CardProps = {
    className?: string;
    image?: string;
    price?: string;
    numberOfPlaces?: string;
    children?: React.ReactNode;
}

export default CardProps;
